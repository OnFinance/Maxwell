diff --git a/src/tools/atlas/create/createDBUser.ts b/src/tools/atlas/create/createDBUser.ts
index 7718dba..e3ecf29 100644
--- a/src/tools/atlas/create/createDBUser.ts
+++ b/src/tools/atlas/create/createDBUser.ts
@@ -90,7 +90,9 @@ export class CreateDBUserTool extends AtlasToolBase {
             content: [
                 {
                     type: "text",
-                    text: `User "${username}" created successfully${shouldGeneratePassword ? ` with password: \`${password}\`` : ""}.`,
+                    // Never echo a generated password: the tool result is stored in the MCP client transcript,
+                    // which the keychain-based log redaction does not cover.
+                    text: `User "${username}" created successfully${shouldGeneratePassword ? " with an auto-generated password. The password is not included in this response; reset it in Atlas (Database Access) to obtain credentials for this user" : ""}.`,
                 },
             ],
         };
diff --git a/tests/integration/tools/atlas/dbUsers.test.ts b/tests/integration/tools/atlas/dbUsers.test.ts
index e7518ff..58c39e0 100644
--- a/tests/integration/tools/atlas/dbUsers.test.ts
+++ b/tests/integration/tools/atlas/dbUsers.test.ts
@@ -104,25 +104,20 @@ describeWithAtlas("db users", (integration) => {
                 expect(elements).toHaveLength(1);
                 expect(elements[0]?.text).toContain("created successfully");
                 expect(elements[0]?.text).toContain(userName);
-                expect(elements[0]?.text).toContain("with password: `");
-
-                const passwordStart = elements[0]?.text.lastIndexOf(":") ?? -1;
-                const passwordEnd = elements[0]?.text.length ?? 1 - 1;
-
-                const password = elements[0]?.text
-                    .substring(passwordStart + 1, passwordEnd - 1)
-                    .replace(/`/g, "")
-                    .trim();
+                expect(elements[0]?.text).toContain("auto-generated password");
+                expect(elements[0]?.text).not.toContain("with password: `");

                 expect(integration.mcpServer().session.keychain.allSecrets).toContainEqual({
                     value: userName,
                     kind: "user",
                 });

-                expect(integration.mcpServer().session.keychain.allSecrets).toContainEqual({
-                    value: password,
-                    kind: "password",
-                });
+                const { allSecrets } = integration.mcpServer().session.keychain;
+                const generatedPasswords = allSecrets.filter((secret) => secret.kind === "password");
+                expect(generatedPasswords.length).toBeGreaterThan(0);
+                for (const { value } of generatedPasswords) {
+                    expect(elements[0]?.text).not.toContain(value);
+                }
             });

             it("should add current IP to access list when creating a database user", async () => {
