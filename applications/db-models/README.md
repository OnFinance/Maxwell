# db-models

Shared Python data-model library (SQLAlchemy models) consumed by internal services. Built from the
OnFinance/onfinance_db_model_master repository (public). Environment: dev only (library, no runtime). Fixture
application for Maxwell's static probes (schemas, SDLC, dev environment).
