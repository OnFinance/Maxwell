# mcp-gateway

Internal MongoDB MCP (Model Context Protocol) gateway that lets ExampleCo's AI agents query the client-data and order
stores. Built from the OnFinance/mongodb-mcp-server repository (public). Environments: dev (docker compose), qa and
prod (EKS ap-south-1). Data classes: pii, financial. This is a fixture application for Maxwell's e2e runs.
