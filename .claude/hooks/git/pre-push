#!/usr/bin/env bash
# Maxwell git pre-push: full validation and tests before anything leaves the machine.
set -euo pipefail
cd "$(git rev-parse --show-toplevel)"
npm run --silent validate
npm test --silent
