---
# GENERATED by .claude/scripts/sync-agents-mirror.mjs from .claude/ — do not edit; run `npm run sync:agents`
description: >-
  Run the full Maxwell validation suite (schemas, layout, data, frontmatter,
  workflows, mirror) and report failures by file
x-maxwell:
  workflow: validate
  touches: []
  readsCredentials: false
---
Run `npm run validate` in the workspace root. If it fails, group the failures by file, explain each in one line
citing the schema rule that was violated, and propose the minimal content fix for each (never a schema change).
Do not modify any file unless the user asked you to fix things; if they did, fix content only, re-run
`npm run validate`, and report the final exit status. $ARGUMENTS
